#-- 5.realiza una funcion que permita rotar una lista -- 

i=input("ingrese lista: ")
n=input("numero de rotaciones: ")
import collections
d = collections.deque(i)
d.rotate(-n)
print d

n=input("numero de rotaciones: ")
import collections
d = collections.deque(i)
d.rotate(-n)
print d

n=input("numero de rotaciones: ")
import collections
d = collections.deque(i)
d.rotate(-n)
print d

n=input("numero de rotaciones: ")
import collections
d = collections.deque(i)
d.rotate(-n)
print d